name: tv_flix
description: "TV Flix - Movies & Series Streaming App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  html: ^0.15.4
  chewie: ^1.8.0
  video_player: ^2.8.2
  cached_network_image: ^3.3.1
  flutter_spinkit: ^5.2.0

flutter:
  uses-material-design: true
